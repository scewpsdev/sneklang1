//This is a line comment
/*
	This is a block comment
*/
load(cmd);
load(file);
load(string);
load(array);

cmd.write(a = null);
cmd.write(b = 5);
cmd.write(c = 'c');
cmd.write(d = true);
cmd.write(e = 0.5);
cmd.write(f = "hello world");
cmd.write(g = [1, 2, 3, 4, 5]);

content = file.open("input.txt").read();
numbers = <i32[]>(string.split(content, '\n'));
file.open("output.txt").write(numbers);
cmd.write(sum = array.sum(numbers));
if true {
	cmd.write("true");
} else {
	cmd.write("false");
}

while sum = i = 0 < numbers.length {
	i %= numbers.length;
	sum += numbers[i++];
}
cmd.write("Sum: " + sum);

l = 6;
r = 3;
l + r;
l - r;
l * r;
l / r;
l % r;
l << r;
l >> r;
l & r;
l | r;
l ^ r;
l == r;
l < r;
l <= r;
l > r;
l >= r;
l && r;
l || r;