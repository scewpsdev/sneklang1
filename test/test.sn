// This is a line comment
/* This is a block comment */

decl putchar(*i32 Char)
decl printf(i8 format)

putchar(b = 99)
putchar(c = 'c')
putchar(100 + (d = true))
putchar(i64(e = 99.5))
printf(f = "\nhello world\n")

letter = if c == 'd' 'Y' else 'N'
if (c == 'c') {
	putchar('-')
}
putchar(letter)
putchar('\n')

i = 0
while i++ < 5 {
	putchar('W')
}
putchar('\n')

i = 0
loop {
	if i++ >= 5 break
	putchar('V')
}
putchar('\n')

l = 6; r = 3
(l + r) + (l - r) + (l * r) + (l / r) + (l % r)
//(l << r) + (l >> r) + (l & r) + (l | r) + (l ^ r)
(l == r) + (l < r) + (l <= r) + (l > r) + (l >= r) + (l && r) + (l || r)
